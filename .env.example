# Paper Pilot — 环境变量示例
# 复制为 .env 并填入真实值：  cp .env.example .env
# .env 已被 .gitignore 忽略，不会提交到仓库。

# OpenAI API Key（必填，Paper Pilot 与 RAG MCP 都可能用到）
OPENAI_API_KEY=

# 若 RAG MCP 使用 Azure OpenAI，在 .env 中配置并在 config/settings.yaml 的 mcp.connections.rag_server.env 里引用
# AZURE_OPENAI_API_KEY=
# AZURE_OPENAI_ENDPOINT=https://xxx.openai.azure.com

# 可选：当使用独立 RAG 项目时，可用 RAG_PROJECT_ROOT 在 settings.yaml 里写 cwd/command
# RAG_PROJECT_ROOT=C:/code/python/MODULAR-RAG-MCP-SERVER
